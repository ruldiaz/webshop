<configuration>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>
                %-4relative [%thread] %-5level %logger{30} - %msg%n
            </pattern>
        </encoder>
    </appender>

    <!-- Set the root logger to INFO, only displaying INFO level logs -->
    <root level="INFO">
        <appender-ref ref="CONSOLE" />
    </root>

    <!-- Adjust logging level for specific packages -->
    <logger name="org.mongodb.driver.connection" level="INFO" additivity="false">
        <appender-ref ref="CONSOLE" />
    </logger>

    <!-- Set Jetty server logs to INFO level -->
    <logger name="org.eclipse.jetty" level="INFO" />

</configuration>
